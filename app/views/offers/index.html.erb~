<h1><%if current_admin %>
<%= link_to "Новая новость", new_offer_path %>
<%end%>
</h1>
<h1>Новости</h1>
<div class="row">
<% @list.each do |element| %>
	<div class="col-md-6">
<div class="col-md-2">
		<%= image_tag element.avatar.url(:thumb), 'width' => '100px', 'height' => '100px' %>
	</div>
	<div class="col-md-10">
		<h2><%= link_to element.title, element %></h2>
		<p><%= element.short_description.html_safe if elenent.short_description %><br>
		<right><%= link_to "Подробно", element %></right></p>
<%if current_admin %>
	<table>
		<tr>
<td colspan="2">  		
			<%= link_to "Изменить", edit_offer_path(element.id) %>
			<%= link_to "Удалить", element, method: :delete %>
		</td></tr>
	</table>
	<%end%>
	</div>
	
	</div>
<% end %>
</div>

